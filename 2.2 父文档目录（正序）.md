.action{$docid:=.id}
.action{$docArray:=(queryBlocks "SELECT * FROM blocks WHERE id='?'" $docid)}

.action{/*获取所有子文件清单*/}
.action{$childFileList:= (queryBlocks "SELECT * FROM blocks WHERE type= 'd' AND path like '%/?/______________-_______.sy' Order BY created" $docid)}
.action{/*获取母文件目录清单*/}
.action{$existTOCList:=(queryBlocks "SELECT * FROM blocks WHERE subtype='h3' AND root_id='?'" $docid)}


.action{range $childFile:=$childFileList}
	.action{$existFlag:=0}
	.action{range $existTOC:=$existTOCList}
		.action{/*每个子文件的标题，逐个与母文件已有的目录清单做对比*/}
		.action{if eq $existTOC.Content $childFile.Content}
			.action{/*如果发现相同的，即设置标志位，并停止循环*/}
			.action{$existFlag = 1}
			.action{break}
		.action{end}
	.action{end}
	.action{/*如果标志位为0，说明该子文件标题不在母文件目录中*/}
	.action{if eq $existFlag 0}
	.action{/*输出该子文件标题及摘要*/}
### ((.action{$childFile.ID} ".action{$childFile.Content}"))
		.action{$hl:= (queryBlocks "SELECT * FROM blocks WHERE subtype='h2' and parent_id='?' and content LIKE '%摘要%'" $childFile.ID)}
		.action{range $h:=$hl} 
{{SELECT * FROM blocks WHERE parent_id='.action{$h.ID}'}}
		.action{end}
	.action{end}
.action{end}